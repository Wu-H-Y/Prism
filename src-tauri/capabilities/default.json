{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions for video player",
  "windows": ["main"],
  "permissions": ["core:default"]
}
